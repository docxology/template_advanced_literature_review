# Recorded Deep-Research Report

`recorded_report.json` is the offline input for Stage 08. Its report body is a
synthetic demonstration summary; its citation entries are metadata-only links
to real primary papers. Provider/job/trace fields explicitly identify curation,
not a paid provider execution. The fixture lets CI exercise request/result
normalization and artifact persistence without credentials, billing, or live
network access.
